const { ResponseHandler, handleResponse } = require('./responseHandler')
const { ErrorHandler, handleError } = require('./errorHandler')

module.exports = {
    ResponseHandler,
    ErrorHandler,
    handleResponse,
    handleError,
}
